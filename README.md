# 단어 테스트장

웹 링크 : https://yoonjiho0421-art.github.io/text-test/
웹 링크에서 다운로드 가능합니다

## 버전 관리

앱 버전은 **Git 태그를 단일 기준(source of truth)** 으로 사용합니다.
`1.1.21` 같은 태그를 생성하면 GitHub Actions가 태그의 버전을 `index.html`, `manifest.json`, `sw.js`, Release ZIP에 자동으로 반영합니다.

앱 하단에도 같은 버전이 표시됩니다.

현재 버전: **1.1.21**
